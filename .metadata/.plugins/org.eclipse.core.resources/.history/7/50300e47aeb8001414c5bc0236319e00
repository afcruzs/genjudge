package unal.edu.gpj

import org.springframework.web.multipart.commons.CommonsMultipartFile

class AdminController {

    def index() { }
	
	def uploadProblem(){}
	
	def doUploadProblem(){
		
		/*def bytesFile = request.getFile('file').getBytes()
		def name = params.titleInput
		def newProblem = new Problem(name:name,pdfDescription:bytesFile)
		newProblem.save()*/
		def testCases = request.getMultiFileMap().testCases
		def names = request.getFileNames()	
		
		while (iterator.hasNext()) {
			String key = (String) iterator.next();
			// create multipartFile array if you upload multiple files
			multipartFile = (CommonsMultipartFile) multipartRequest.getFile(key);
		}
		
		println "SIZE; " + testCases.size()
		for(int i=0; i<testCases.size(); i++){
		//	println i
			//println testCases[i].getContentType()
		}

		render "LOL" + testCases.size()
	}
	
	def xd(){
		render Problem.list().size()
	}
}
